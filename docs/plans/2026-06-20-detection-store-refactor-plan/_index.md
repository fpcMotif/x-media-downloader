# DetectionStore Refactor — Execution Plan

Date: 2026-06-20
Source design: `docs/superpowers/specs/2026-06-20-detection-store-refactor-plan.md`
(read it for full rationale + the grilled decisions; this file is the executable
task list).

## Summary

Code-quality refactor of the syndication video-recovery feature (ADR-0015, reviewed
= 0 bugs) and the overlay detection state it touches. Three isolated milestones:
M1 polish → M2 behavior-preserving DetectionStore extraction → M3 unify `item.id`
on pure media-key (behavior change, ADR'd).

## Preconditions (verify BEFORE starting — this tree was contested)

- `git status` is committable and the concurrent Droid/Factory tool is **idle**
  (no `src/**` or `coverage/` churn for several minutes). The plan rewrites
  `src/entrypoints/overlay.content/index.tsx`; a live writer on it WILL conflict.
- A clean baseline commit exists (the refactor commits land on top of it).
- `coverage/` and `backend/coverage/` are git-ignored or otherwise excluded — they
  are generated by `test:coverage` and must not enter commits.

## Manual gates (the loop CANNOT auto-satisfy these — pause for the human)

- **M2 done** (after task-003): a real-extension `/verify` run on
  https://x.com/ooaoau/status/2068286123399676218 — count + download + hover +
  recovery behave IDENTICALLY to pre-refactor.
- **M3 done** (after task-005): a `/verify` run confirming correct count, working
  video download, no double-count on a multi-photo tweet, and intact
  badge/launcher outcome correlation (ADR-0014 path).

## Execution Plan

```yaml
tasks:
  - id: "001"
    subject: "Track and clear settleRenderedScan timers"
    slug: "track-settle-timers"
    type: refactor
    depends-on: []
  - id: "002"
    subject: "Characterization tests for makeDetectionStore"
    slug: "detection-store"
    type: test
    depends-on: []
  - id: "002"
    subject: "Implement makeDetectionStore (behavior-preserving)"
    slug: "detection-store"
    type: impl
    depends-on: []
  - id: "003"
    subject: "Repoint overlay call sites to the DetectionStore"
    slug: "repoint-overlay-callsites"
    type: refactor
    depends-on: ["002"]
  - id: "004"
    subject: "New-semantics tests for media-key identity"
    slug: "media-key-identity"
    type: test
    depends-on: ["003"]
  - id: "004"
    subject: "Unify item.id on pure media-key"
    slug: "media-key-identity"
    type: impl
    depends-on: ["003"]
  - id: "005"
    subject: "Update id consumers and split('-') test helpers; re-verify"
    slug: "update-id-consumers"
    type: refactor
    depends-on: ["004"]
  - id: "006"
    subject: "Write ADR-0016 (media-key identity)"
    slug: "write-adr-0016"
    type: docs
    depends-on: ["004"]
```

## Batches (suggested)

1. **M1 + M2-store:** task-001, task-002-test → task-002-impl (Red-Green pair).
2. **M2-repoint:** task-003. → MANUAL /verify gate.
3. **M3:** task-004-test → task-004-impl (Red-Green pair), task-006 (ADR).
4. **M3-consumers:** task-005. → MANUAL /verify gate.

## Definition of done

All tasks `completed`, both manual `/verify` gates passed, ADR-0016 present,
`bun run check` green (modulo any concurrent-tool coverage gaps outside this plan).
