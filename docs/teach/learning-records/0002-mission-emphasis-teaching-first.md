# Mission emphasis: master the thinking; code is the vehicle

Session 5: the user sharpened the mission — the goal is **mastery of monadic & category-theoretic
thinking in TypeScript**, with Effect-TS and this codebase as the case study, not the deliverable.
They explicitly said code changes are fine but secondary: "showcase in your teaching."

Why it steers future sessions: prefer runnable, deletable demos in `src/core/teach/` that make a
concept concrete; reference the real code as the "before"; do NOT frame large production refactors
(e.g. migrating all of `DriveDeps`/`cfg` DI to Layers) as the work — frame them as *judgment
exercises*. Lessons should keep pairing each abstraction with (1) its categorical meaning and
(2) the engineering call of when to apply vs decline it — the user values that "defend it" axis.

Cross-link: [[MISSION.md]] (Why + constraints updated to match).
